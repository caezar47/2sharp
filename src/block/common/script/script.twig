<!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
<!--[if lt IE 9]>
	<script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
	<script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
<![endif]-->
<script src="{{path.js}}/jquery-3.2.1.min.js" ></script>
{% if map == "view" %}
<script src="//api-maps.yandex.ru/2.1/?lang=ru_RU"></script>
<script type="text/javascript" src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAnNznmhY_wMxtm4mF97XXIsQ_qkZlS0wg"></script>
{% endif %}
<script src="{{path.js}}/document-ready.js" ></script>
<script src="{{path.js}}/svg4everybody.min.js" ></script>
<script>
svg4everybody();
document.createElement("picture");
{% if wow == "view" %}
new WOW().init();
{% endif %}
{% if aos == "view" %}
AOS.init({
	offset: 0,
	duration: 500,
	easing: 'ease',
	delay: 300,
	once: true,
	disable: function () {
    	var maxWidth = 768;
    	return window.innerWidth < maxWidth;
	}
});
{% endif %}
var lazyLoadInstance = new LazyLoad({
    elements_selector: ".lazy"
});
</script> 
{% if hoverEffect == "view" %}
{# <script src="{{path.js}}/imagesloaded.pkgd.min.js"></script> #}
<script src="{{path.js}}/three.min.js"></script>
<script src="{{path.js}}/TweenMax.min.js"></script>
<script src="{{path.js}}/hover-effect.umd.js"></script>
<script>
	// imagesLoaded( document.querySelectorAll('.card__effect img'), () => {
	// 	document.body.classList.remove('loading');
	// });
	Array.from(document.querySelectorAll('.card__effect')).forEach((el) => {
		const imgs = Array.from(el.querySelectorAll('img'));
		//console.log(el.dataset.ratio);
		new hoverEffect({
			parent: el,
			intensity: el.dataset.intensity || undefined,
			speedIn: el.dataset.speedin || undefined,
			speedOut: el.dataset.speedout || undefined,
			easing: el.dataset.easing || undefined,
			hover: el.dataset.hover || undefined,
			image1: imgs[0].getAttribute('src'),
			image2: imgs[1].getAttribute('src'),
			displacementImage: el.dataset.displacement,
			imagesRatio:  el.dataset.ratio,
			imagesRatio: 725/1290,
		});
	});
</script>
{% endif %}
{% if hoverEffectT == "view" %}
<script src="{{path.js}}/app_build.js" ></script>
{% endif %}
<script src="{{path.js}}/jquery.sticky.min.js" ></script>
<script type="text/javascript">
    $(".navbar__collapse-row.is--index").sticky({
        topspacing: 40,
        stopper: '.footer__block',
        minwidth: 1200,
        marginbottom: 20,
        styler: 'is-sticky',
        //sticktype: 'directly',
    });
    $('.navbar__block.is--second').stickySidebar({
        topSpacing: 40,
        container: '.content-block__wrapper-cols',
        sidebarInner: '.navbar__block.is--second',
    });
</script>